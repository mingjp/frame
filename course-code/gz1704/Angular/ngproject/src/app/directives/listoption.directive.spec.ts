import { ListoptionDirective } from './listoption.directive';

describe('ListoptionDirective', () => {
  it('should create an instance', () => {
    const directive = new ListoptionDirective();
    expect(directive).toBeTruthy();
  });
});
